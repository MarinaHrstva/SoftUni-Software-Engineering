function fromRadiansToDegrees(radiansInput) {
    let radians = Number(radiansInput);
    let degrees = radians * 180 / Math.PI;


    console.log(degrees.toFixed(0))
}

function fromRadiansToDegrees("3.1416");
